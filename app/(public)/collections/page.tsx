import { CollectionsList } from '@/components/public/collections-list';

export const metadata = {
    title: 'Colecciones | Varmina',
    description: 'Explora nuestras hermosas colecciones de joyas.',
};

export default function CollectionsPage() {
    return <CollectionsList />;
}
